# Pipeline. I push code, GitHub Actions builds the image, deploys it to an EC2 instance over SSH, Nginx reverse-proxies the app, and it's served over HTTPS with automatic certificate renewal.

## https://flask3rd.duckdns.org
